Source: https://www.nginx.com/blog/deploying-nginx-nginx-plus-docker/

# Create a NGINX container and interact

# Creates a container based on NGINX image with name mynginxserver
# -p Maps port 80 in the container to port 80 on the host machine.
# -d Specifies detached mode, runs until it's stopped
docker run --name mynginxserver -p 80:80 -d nginx 

# Check our running containers
docker ps

# Make an HTTP request 
curl http://localhost

# Open interactive shell inside the container
docker exec -it [container_ID] sh(Alpine Linux)/bash(Debian systems)

# Install nano
apt-get update
apt-get -y install nano

# Modify index page and check changes
cd /usr/share/nginx/html 
nano index.html 
